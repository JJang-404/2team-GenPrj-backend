

designApi.py 에서는 아래처럼 호출된 것을 받습니다.기존의 구조와 동일하게 만들어 주세요.

class DesignApi extends BaseApi {
  async saveDesignProfile(payload) {
    alert('디자인 메타 저장 API 호출\n\n' + JSON.stringify(payload, null, 2));

    return this.post('/design/saveprofile', payload, '디자인 메타 저장에 실패했습니다.');
  }
}



save_design_profile 함수에서 저장할 테이블을 만들어 주세요.
테이블 이름은 design_profile_tbl로 해주시고,
컬럼은 profile_id(key) user_id, profile_jason 형태로 만들어 주면 될것 같습니다.
참고로  CONSTRAINT valid_json CHECK (json_valid(profile_jason)) 이것을 쓰는게 더 좋으면 그렇게 하면 될것 같습니다.
user_id는 json에 같이 들어 있습니다. 


save_design_profile 함수에서 
프론트엔드에서 
프로파일 저장시 payload에 이 profile_id도 넘어 옵니다.
새 디자인의 경우에는 profile_id를 0으로 하고,
아닌 경우에는 값을 주어서 저장시에 
서버에서 0이면 insert, 0보다 크면 update를 할수 있도록 해야 합니다.

design_profile_tbl
profile_id 당  AI 이미지가 생성될수 있습니다.
그러므로 design_profile_tbl 에
user_images_tbl 의 image_id 를 
저장하는   ai_image_id  를 만들어 주세요,
이값은 ai이미지가 없을수도 있으므로
null 값도 허용 해야 합니다.

_vlm_fusion_prompt = (
    "You are a VLM Prompt Engineer for SD 3.5.\n"
    "Step 1: Analyze the input image's layout and color.\n"
    "Step 2: Combine it with the user's prompt: {user_input}.\n"
    "Step 3: Follow these STRICT RULES:\n"
    "- First 60 tokens: environment + composition + inherited visual traits.\n"
    "- Subject > Style > Composition > Lighting > Color > Details.\n"
    "- For background scenes: Ensure 'empty scene, no people' is prioritized.\n"
    "Return only JSON with 'positive_prompt' and 'negative_prompt'."
)

gemma4ollama.py 는 올라마에 접속해서 
이미지와 프롬프트/포지티브프롬프트/네가티브프롬프트
를 받아서 위의 _vlm_fusion_prompt 를 기본 베이스로 해서
프롬프트를 생성하는 것입니다.
(올라마에 이미지까지 보내기)

modelApi.py의 _extract_text_from_image_base64 대신에
이미지에서 텍스트를 뽑아서 자동으로 프롬프트와 함께 보내면
diffusion3.5에 보내줄 프롬프트를 생성하는 기능까지 해야 합니다.
접속은 backend.ini의 

[ollama]
# Ollama 모델 이름
model_name =gemma4:e4b
# Ollama 서버 주소 (끝에 / 있어도 코드에서 자동 정리)
ollama_url = http://nabidream.duckdns.org:11139
에 접속을 하고 현재 코드는 예제 코드 입니다.
저장된 이미지가 아니라 이미지를 리액트 클라이언로부터 받고 이를 
다시 ollama로 보내서 작업을 해야 합니다.




 commfyui.py 의 florence_vlm 함수를 
comfyui 에 접속해서  data/comfyui/florencevlm.json/model/makebgimagecomfyui/jobs 
파일에 이미지를 넣고 생성된 문자열을 가져 오는 것을 만들어 주세요.

comfyui.py
florence_vlm 의 



/project/2team-GenPrj-backend/data/test/iceamericano.jpg 파일로 테스트 가능 


"You are a VLM Prompt Engineer for SD 3.5.\n"
            "Step 1: Analyze the input image's layout and color.\n"
            "Step 2: Extract any visible text from the image and include it in the scene understanding.\n"
            "Step 3: Combine image analysis with the user's request: {user_input}.\n"
            "Step 4: Follow these STRICT RULES:\n"
            "- First 60 tokens: environment + composition + inherited visual traits.\n"
            "- Subject > Style > Composition > Lighting > Color > Details.\n"
            "- For background scenes: Ensure 'empty scene, no people' is prioritized.\n"
            "Return only JSON with 'positive_prompt' and 'negative_prompt'."